# Полная сборка: локальный Whisper плюс всё из requirements-lite.txt.
# Лёгкая сборка (только облако) ставится из requirements-lite.txt.
-r requirements-lite.txt

# Распознавание речи на этой машине.
faster-whisper>=1.2

# --- Ускорение на видеокарте NVIDIA (необязательно, только Windows/Linux) ---
# Без этих пакетов приложение само откатится на CPU.
nvidia-cublas-cu12; sys_platform != "darwin"
nvidia-cudnn-cu12; sys_platform != "darwin"
