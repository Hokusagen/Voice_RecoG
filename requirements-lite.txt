# Лёгкая сборка: распознавание и правка только в облаке, без локальных моделей.

# Интерфейс: окно HUD, трей, анимации.
PySide6-Essentials>=6.7

# Захват звука и работа с массивами.
sounddevice>=0.5
numpy>=2.0

# Глобальные горячие клавиши и буфер обмена.
keyboard>=0.13; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
pynput>=1.7; sys_platform != "win32"

# Живое стекло: DXGI Desktop Duplication (на гибридной графике сам откатится
# на GDI, пакет всё равно нужен для проверки). Только Windows.
bettercam>=1.0; sys_platform == "win32"

# Запросы к Ollama и облаку.
requests>=2.32

# Сборка в автономное приложение (нужна только для build).
pyinstaller>=6.10
